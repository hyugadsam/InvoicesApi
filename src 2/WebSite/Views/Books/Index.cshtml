@model IEnumerable<Dtos.Common.BookDto>
@{
    ViewData["Title"] = "Books";
}

<div class="row">
    <div class="col-12">
        <a asp-action="Create" style="float:right" class="btn btn-success m-2" > <i class="bi bi-plus-circle-fill"></i> Crear Book </a>
    </div>
    <div class="col-12 text-center">
        <div class="card-header">
            <h1>Lista de Libros</h1>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                @if (Model.Count() > 0)
               {
                   <table class="table table-bordered" id="a1">
                       <thead>
                           <tr>
                               <th>id</th>
                               <th>Titulo</th>
                               <th>Fecha de creacion</th>
                               <th>Acciones</th>
                           </tr>
                       </thead>
                       <tbody>
                           @foreach (var item in Model)
                          {
                              <tr>
                                  <td>@item.BookId</td>
                                  <td>@item.Title</td>
                                  <td>@item.CreateDate.ToShortDateString()</td>
                                  <td>
                                      <a class="btn btn-primary" asp-action="Update" asp-route-BookId="@item.BookId"> Editar </a>
                                      <a class="btn btn-info" asp-action="Details" asp-route-BookId="@item.BookId"> Detalle </a>
                                      <a class="btn btn-danger" asp-action="Delete" asp-route-BookId="@item.BookId"> Eliminar </a>
                                  </td>
                              </tr>
                          }
                       </tbody>
                   </table>
               }
                else
                {
                    <p>No hay registros</p>
                }
            </div>
        </div>
    </div>
</div>

@if (ViewBag.Message != null)
{
    <script type="text/javascript">
        window.onload = function () {
            alert("@ViewBag.Message");
        };
    </script>
}

