@model IEnumerable<Dtos.Common.AuthorDto>
@{
    ViewData["Title"] = "Authors";
}

<div class="row">
    <div class="col-12">
        <a asp-action="Create" style="float:right" asp-controller="Authors" class="btn btn-success m-2" > <i class="bi bi-plus-circle-fill"></i> Crear Autor </a>
    </div>
    <div class="col-12 text-center">
        <div class="card-header">
            <h1>Lista de autores</h1>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                @if (Model.Count() > 0)
               {
                   <table class="table table-bordered" id="a1">
                       <thead>
                           <tr>
                               <th>id</th>
                               <th>Nombre</th>
                               <th>Acciones</th>
                           </tr>
                       </thead>
                       <tbody>
                           @foreach (var item in Model)
                          {
                              <tr>
                                  <td>@item.AuthorId</td>
                                  <td>@item.Name</td>
                                  <td>
                                      <a class="btn btn-primary" asp-controller="Authors" asp-action="Update" asp-route-AuthorId="@item.AuthorId"> Editar </a>
                                      <a class="btn btn-info" asp-controller="Authors" asp-action="Details" asp-route-AuthorId="@item.AuthorId"> Detalle </a>
                                      <a class="btn btn-danger" asp-controller="Authors" asp-action="Delete" asp-route-AuthorId="@item.AuthorId"> Eliminar </a>
                                  </td>
                              </tr>
                          }
                       </tbody>
                   </table>
               }
                else
                {
                    <p>No hay registros</p>
                }
            </div>
        </div>
    </div>
</div>

@if (ViewBag.Message != null)
{
    <script type="text/javascript">
        window.onload = function () {
            alert("@ViewBag.Message");
        };
    </script>
}

