@model Dtos.Common.FullAuthorDto

@{
    ViewData["Title"] = $"Detalle Autor {Model?.AuthorId}";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>@ViewData["Title"]</h1>

<div class="mt-2">
    <div class="row mt-4">
        <div class="row">
            <div class = "col-md-2 col-sm-2 col-xs-4">
            <h6>@Html.DisplayNameFor(model => model.AuthorId)</h6>
            </div>
            <div class = "col-md-8 col-sm-10 col-xs-8">
                <h6>@Html.DisplayFor(model => model.AuthorId)</h6>
            </div>
        </div>

        <div class="row">
            <div class = "col-md-2 col-sm-2 col-xs-4">
                <h6>@Html.DisplayNameFor(model => model.Name)</h6>
            </div>
            <div class = "col-md-8 col-sm-10 col-xs-8">
                <h6>@Html.DisplayFor(model => model.Name)</h6>
            </div>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-12">
            @if (Model.BooksList?.Count() > 0)
            {
                <table class="table table-bordered">
                     <thead>
                           <tr>
                               <th>id</th>
                               <th>Titulo</th>
                               <th>Fecha de creacion</th>
                           </tr>
                       </thead>
                       <tbody>
                           @foreach (var item in Model.BooksList)
                          {
                              <tr>
                                  <td> @item.BookId </td>
                                  <td> @item.Title </td>
                                  <td> @item.CreateDate.ToShortDateString() </td>
                              </tr>
                          }
                       </tbody>
                </table>
            }
            else
            {
                <p><b> El autor no tiene libros asignados </b></p>
            }
        </div>
    </div>
</div>
<div style="display:flex">
    <a asp-action="Index">Back to List</a>
    <p> &nbsp; || &nbsp; </p>
    <a asp-action="Update" asp-route-AuthorId="@Model.AuthorId"> Edit </a>
</div>
